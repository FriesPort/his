<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mapper.UserMapper">
    <select id="userList" resultType="com.example.vo.systemmanagement.user.UserDisplayVO">
        select user.id,
        user.username,
        user.employee_number,
        user.employee_name,
        user.user_type,
        user.is_use,
        camp.name campus_name
        from user user
        left join campus camp on user.campus_id = camp.id
        <where>
            user.is_use = 1 and user.user_type = #{userDisplayDTO.userType}
            <if test="userDisplayDTO.name!=null and userDisplayDTO.name!=''">
                and user.employee_name=#{userDisplayDTO.name}
            </if>
            <if test="userDisplayDTO.employeeNumber!=null and userDisplayDTO.employeeNumber!=''">
                and user.employee_number=#{userDisplayDTO.employeeNumber}
            </if>
        </where>
    </select>
    <select id="userRoleDisplay" resultType="com.example.vo.systemmanagement.userrole.UserRoleDisplayVo">
        select id role_id, name role_name
        from role
        where id in (select role_id
                     from user_role
                     where user_id = #{userId})
    </select>
    <select id="unownedUserRoleDisplay"
            resultType="com.example.vo.systemmanagement.userrole.UserRoleDisplayVo">
        select id role_id, name role_name
        from role
        where id not in (select role_id
                         from user_role
                         where user_id = #{userId})
    </select>
</mapper>